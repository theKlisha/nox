[package]
name = "nox"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[[bin]]
name = "nox"
test = false
bench = false

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
